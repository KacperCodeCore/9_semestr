/// <reference types="cypress" />

context('biedronka', () => {
    beforeEach(() => {
        cy.visit('https://en.wikipedia.org/wiki/Main_Page/index.html', { timeout: 10000 })
    })


    it('powinien przycisnąć przycisk', () => {

        // cy.get('.side_categories')
        //     .contains('Travel')
        //     .click()
        // cy.get('h1').contains("travel")

    })

    it('wyszukaj', () => {

        // cy.get('.side_categories')
        //     .contains('Travel')
        //     .click()
        // cy.get('h1').contains("travel")

    })

    it('', () => {

    });
})

